# 11c — Featured & Special Products Re-Grounding Verification (2026-07-02)

> **Re-grounding pass over the committed Prompt-11 work (`d922868`) *and* its `11b` post-execution note.** Following the `06c`/`07c`/`08c`/`09c`/`10c` precedent for a prompt that shipped with a full `b` note: **re-derive every claim in `11b` and in the Prompt-11 spec's §9/§11 independently from live source, seed data and a fresh build**, rather than trusting the note. Every material assertion was re-checked by **re-reading the eight changed files plus the consumed surfaces, re-running the git/grep checks, re-inspecting the seeded `db.json`, and re-building from scratch — twice (Prompt-11 and its immediate parent)** — not by echoing `11b`. **No application code, tokens, docs or config were modified by this pass** (this note is the only new artifact). Bottom line: **all nine §9 acceptance criteria reproduce, and all `11b` claims hold** — the additive gold `#fa9c4c` **"Special"** ribbon on the one canonical `storefront/ProductCard` (top-left, `pointer-events:none`, shown iff `product.special === true`, no wishlist-heart overlap), the new dual-mode `apiService.products.getSpecial()` mirroring `getFeatured` (mock `GET /products?special=true`, Laravel `GET /products/special` via `extractData`) as the **single shared data source** for the homepage Special band (`getSpecial(8)`) and the `/special-offers` collection (`getSpecial(60)`), `FeaturedProducts.js` repurposed into a thin reusable band over the shared card (no forked local card / "% OFF" / "Add to Cart"), and `/special-offers` rebuilt from a deals/coupons page into the **Special Products collection** — a NEBM blue `--sf-gradient-primary` hero, gold "Curated picks" kicker, curation `CategoryTabs`, a badged grid of the shared card, skeletons and a graceful empty state → `/products`, with **no** coupons / countdown / deal-of-the-day / "% off" urgency. A fresh `CI=true react-scripts build` **compiles successfully** at **JS 384.05 kB gzip / CSS 45.63 kB gzip** — reproducing `11b`'s absolute figures byte-for-byte — and a second build of the **immediate parent `e353dff`** (tidied Prompt-10) at **JS 387.16 kB / CSS 47.51 kB** independently reproduces `11b`'s claimed **−3.11 kB JS / −1.88 kB CSS** shrink. The diff's **434 ins / 1,721 del (−1,287 lines)** across 8 files reproduces exactly.
>
> **Companion, not a rewrite.** Following `06c`–`10c`, this note verifies the committed work alongside it and edits nothing — the committed `ProductCard.*`, `FeaturedProducts.*`, `SpecialOffers.*`, `Home.js` and `api.js` are left byte-identical; the findings in §7 are recorded for the owning prompts.
>
> **Prime directive (unchanged):** *analyze → reuse → refactor → rename → redesign → extend*. Prompt-11 was **reuse + subtract**: make the one canonical card carry an additive badge (so it appears on every surface that renders it), give Featured/Special one shared dual-mode source, and delete the coupons/countdown/deal-of-the-day machinery plus two forked local cards. This pass only re-reads, re-greps, re-inspects the seed, re-builds and re-asserts.

---

## 1. Method — re-execute, don't trust

`11b` asserts a two-way verification: a clean build **and** a live Chromium runtime probe (`/special-offers` h1 "Special Products"; "Curated picks" computed gold `rgb(250,156,76)`; hero computed blue gradient `#1885d8→#1069b0`; 10 badged special cards each with an "Add to Enquiry List" button; "All" + 10 curation tabs; first card → normal PDP; add-to-enquiry growing `localStorage["cart"]`; the homepage bands showing 9 gold badges across 16 shared cards; mock `/products?special=true` → 10; the additive check `/products?category=wpc-louvers` still listing the special WPC louver; zero coupon/countdown/deal/%-off/Buy-Now copy; console error-free). Rather than accept those, this pass re-derived them:

1. **Diff footprint** — `git show d922868 --numstat` (blast radius) + `git status --porcelain` (working tree clean) + lineage.
2. **Source re-read** — every value the spec §9 / §11 and `11b` assert read straight out of the **written** `storefront/ProductCard.js` / `.module.css`, `services/api.js` (`getSpecial`), `FeaturedProducts.js` / `.module.css`, `SpecialOffers.js` / `.module.css`, and `Home.js`.
3. **Contract check** — the consumed surfaces re-read at source (`products.getFeatured`/`getSpecial` + `extractData`, `categories.getAll`, `utils/helpers.js` `buildCartItem`/`getProductMinPrice`/`productPath`, `utils/categories.js` `categoryParam`, `hooks/useCart` add path, `context/WishlistContext`, `storefront/PriceBlock`, `storefront/StarRating`, `theme/storefront-tokens.css` palette, `db.json` seed) to confirm every symbol/prop the pages call exists and is unchanged.
4. **Seed re-inspection** — `db.json` re-grepped for the `special:true` / `featured:true` product counts that drive the rendered card totals.
5. **Grep** — a fresh sweep of `src` for cart/deal/coupon/countdown/urgency wording, distinguishing the **changed** files (visible copy) from the untouched surfaces (listing, PDP, admin, deals-config util) that still legitimately carry those terms.
6. **Build** — a fresh `CI=true npm run build` from the current tree, **and** a second build of the immediate parent `e353dff` to reproduce `11b`'s bundle delta exactly.

Every result below is the actual output of those runs — the browser-only observations in `11b` (computed `rgb()`, the add-to-enquiry `localStorage` increment, "console error-free," the badge/tab counts, the additive-listing check) are re-grounded **from source + build + seed**, consistent with how `06c`–`10c` re-ran build/seed rather than the UI (see §8).

---

## 2. Diff footprint & lineage — reproduce exactly

- **Footprint.** `git show d922868 --numstat` → exactly **eight** files: `FeaturedProducts.js` (**60/83**), `FeaturedProducts.module.css` (**66/37**), `storefront/ProductCard.js` (**12/5**), `storefront/ProductCard.module.css` (**21/0**), `Home.js` (**8/9**), `SpecialOffers.js` (**110/569**), `SpecialOffers.module.css` (**141/1,018**), `services/api.js` (**16/0**) — commit total **434 ins / 1,721 del (net −1,287)**, reproducing `11b` byte-for-byte. Routes, providers, `db.json`, `utils/*`, `PriceBlock`, `StarRating` and the admin palette are **absent from the diff**.
- **Lineage.** `d922868^` = `e353dff` ("Tidy Prompt-10 homepage leftovers"), whose ancestry runs `e353dff → 9682de9` (the `10c` note) `→ 35bf669` (Prompt-10). So Prompt-11 sits on the **tidied** post-Prompt-10 tree — the correct baseline against which `11b` measures the bundle delta (see §8 for why this differs by ~0.15 kB from `10c`'s recorded `35bf669` figure).
- **Clean tree.** `git status --porcelain` is **empty** after both rebuilds (`build/` is gitignored), and HEAD is back on `analysis/regrounding-verification-note` — so the verification left the tracked tree byte-identical.

---

## 3. What changed — reproduced at source

| File | Change re-grounded from the written file | ✓ |
|---|---|---|
| **`storefront/ProductCard.js`** | Additive gold **"Special"** ribbon: `{product.special === true && <span className={styles.specialBadge} title="Special product">Special</span>}` inside the `.media` link ([:64–68](../src/components/storefront/ProductCard.js)). Purely presentational — never changes price or links; false/missing hides it. The "Add to Enquiry" icon-button (`aria-label`/`title` "Add to Enquiry List", `buildCartItem` → `onAddToCart`, out-of-stock guard, [:101–133](../src/components/storefront/ProductCard.js)) and the clean single price (`PriceBlock price={sellingPrice} showSavings={false}`, [:99](../src/components/storefront/ProductCard.js)) are Prompt-10's, unchanged. Card comment updated to record the badge is now present ([:38–45](../src/components/storefront/ProductCard.js)). | ✅ |
| **`storefront/ProductCard.module.css`** | New `.specialBadge` on `--sf-*` tokens: `background: var(--sf-color-accent)` (`#fa9c4c`), white text, pill, `top/left: var(--sf-space-2)`, `z-index: 1`, `pointer-events: none` ([:36–52](../src/components/storefront/ProductCard.module.css)). Sits **top-left**; the wishlist heart is **top-right** (`.wishlist { right }`, [:54–57](../src/components/storefront/ProductCard.module.css)) — **no overlap**. No `.discountBadge` (grep → 0). | ✅ |
| **`services/api.js`** | New `apiService.products.getSpecial(limit = 12)` mirroring `getFeatured`: mock `GET /products?special=true` sliced to `limit`; Laravel `GET /products/special?limit=` via `extractData()`; `IS_MOCK_API` branch + try/catch preserved ([:914–923](../src/services/api.js)). `getFeatured` ([:898–907](../src/services/api.js)) unchanged. | ✅ |
| **`Home.js`** | Special band repointed from Prompt-10's `getAll().filter(special).slice(0,8)` to the shared **`getSpecial(8)`** ([:81](../src/pages/Home/Home.js)); the redundant client-side `.filter` is gone; `.catch(()=>[])` + `Array.isArray().slice(0,8)` guard kept ([:94–96](../src/pages/Home/Home.js)). The band renders the shared `ProductCard` ([:162–167](../src/pages/Home/Home.js)) with the "Curated picks" kicker → `/special-offers` ([:256–262](../src/pages/Home/Home.js)); Featured (`getFeatured(8)`) already renders the shared card, so it now shows the badge automatically. | ✅ |
| **`FeaturedProducts.js`** | Forked local card (`getProductMinPrice`/`formatCurrency`/"% OFF" `.discountBadge`/"Add to Cart") **deleted**; the band now maps products to the shared `storefront/ProductCard` with `useCart`/`useWishlist` wiring (`buildCartItem` → `addToCart`, [:31–34, :62–67](../src/components/FeaturedProducts/FeaturedProducts.js)) and framer-motion `whileInView` reveals; new optional `subtitle`/`viewAllText` props; **renders nothing when `!products.length`** ([:36](../src/components/FeaturedProducts/FeaturedProducts.js)). | ✅ |
| **`FeaturedProducts.module.css`** | Rewritten on `--sf-*` tokens: section/container/header/title/subtitle/view-all/grid only; dark mode follows `body.dark` automatically ([:3](../src/components/FeaturedProducts/FeaturedProducts.module.css)). Deleted the local-card/discount/wishlist/price CSS. | ✅ |
| **`SpecialOffers.js`** | **Rebuilt** into the Special Products collection. Removed: coupons, the countdown hook, "Deal of the Day", discount-sort, the forked local `ProductCard`/`StarRating`, and the `useDealsConfig` master-toggle gate. Now: guarded `getSpecial(60)` + `categories.getAll()` ([:144–147](../src/pages/SpecialOffers/SpecialOffers.js)), a defensive `special === true` re-filter ([:150–152](../src/pages/SpecialOffers/SpecialOffers.js)), NEBM blue hero + gold "Curated picks" kicker + "Special Products" h1 ([:208–223](../src/pages/SpecialOffers/SpecialOffers.js)), curation `CategoryTabs` over only-represented categories ([:171–176, :246–252](../src/pages/SpecialOffers/SpecialOffers.js)), a result count, a badged shared-card grid ([:262–279](../src/pages/SpecialOffers/SpecialOffers.js)), skeletons, and a graceful empty state → `/products` ([:232–243](../src/pages/SpecialOffers/SpecialOffers.js)). | ✅ |
| **`SpecialOffers.module.css`** | Rewritten (−≈1,000 lines) on `--sf-*` tokens: page shell, blue `--sf-gradient-primary` hero ([:23](../src/pages/SpecialOffers/SpecialOffers.module.css)) + gold `--sf-color-accent` kicker ([:49](../src/pages/SpecialOffers/SpecialOffers.module.css)), token-based responsive tabs (kept), product grid, token skeletons, empty state. Deleted all coupon/countdown/deal-of-the-day/local-card CSS and every per-rule `.dark`. | ✅ |
| **`helpers.js` / `categories.js` / `PriceBlock` / `StarRating`** | **UNCHANGED** — not in the diff. `buildCartItem` ([helpers.js:76](../src/utils/helpers.js)), `getProductMinPrice` ([helpers.js:24](../src/utils/helpers.js)), `productPath` ([helpers.js:103](../src/utils/helpers.js)), `categoryParam` ([categories.js:26](../src/utils/categories.js)) all byte-identical; `PriceBlock` still carries its compare-price logic for the PDP. | ✅ |

---

## 4. The badge, the shared source & the counts — re-derived from the seed

`11b` claims "10 special cards on `/special-offers`," "16 shared cards on the homepage," and "9 gold badges." Re-grounding the numbers against the seed the pages actually read:

- **10 `special:true` + 10 `featured:true`.** `db.json` carries exactly **10** products flagged `special: true` and **10** flagged `featured: true`. `getSpecial(60)` returns all 10 (the mock slices to `limit`); `SpecialOffers.js` re-filters `special === true` ([:150–152](../src/pages/SpecialOffers/SpecialOffers.js)) → **10 badged shared cards**. `getSpecial(8)` and `getFeatured(8)` each slice to 8 → the homepage Special + Featured bands render **8 + 8 = 16** shared cards.
- **The badge is additive, driven purely by the seed flag.** The card shows the ribbon **iff** `product.special === true` ([ProductCard.js:64](../src/components/storefront/ProductCard.js)) — so a *featured* product that also carries `special: true` shows the badge in the Featured band too. `11b`'s live "9 badges across 16 cards" = the Special band's 8 (all special) plus the featured/special overlap surfaced in the first 8 featured; the exact live count is a seed-overlap function, and its **structural basis** — an additive badge on every card whose `special` flag is true, regardless of band — is confirmed at source.
- **One shared data source.** Both the homepage Special band ([Home.js:81](../src/pages/Home/Home.js)) and the collection ([SpecialOffers.js:145](../src/pages/SpecialOffers/SpecialOffers.js)) read `apiService.products.getSpecial(...)` — the redundant Prompt-10 client-side `.filter(special)` is gone, so the band and the page can never diverge on what "special" means.
- **Both surfaces funnel through the shared card's enquiry wiring.** The card emits `buildCartItem(product)` ([ProductCard.js:110](../src/components/storefront/ProductCard.js)) into each page's `handleAddToEnquiry` → `addToCart(cartItem, 1)` ([Home.js:112–117](../src/pages/Home/Home.js), [SpecialOffers.js:193–196](../src/pages/SpecialOffers/SpecialOffers.js), [FeaturedProducts.js:31–34](../src/components/FeaturedProducts/FeaturedProducts.js)), so a quick-add anywhere merges with PDP adds and the `localStorage["cart"]` key is unchanged — the structural basis for `11b`'s live increment.

---

## 5. Prompt-11 §9 acceptance — re-derived against source + seed

Every §9 bullet, with how it was re-checked (all **PASS**):

- **`ProductCard` renders a gold `#fa9c4c` "Special" badge iff `product.special === true`; no overlap with the wishlist heart; false/missing hides it** — `.specialBadge` `background: var(--sf-color-accent)` = `#fa9c4c` ([tokens :35](../src/theme/storefront-tokens.css)), top-left + `pointer-events:none` ([ProductCard.module.css:36–52](../src/components/storefront/ProductCard.module.css)); heart top-right; render guarded by `product.special === true` ([ProductCard.js:64](../src/components/storefront/ProductCard.js)).
- **Special products fetched dual-mode (`getSpecial` mirrors `getFeatured`) via `extractData()`; mock hits `/products?special=true`** — `getSpecial` mock branch `api.get("/products", { params: { special: true } })` sliced to `limit`; Laravel branch `/products/special` via `extractData()` ([api.js:914–923](../src/services/api.js)).
- **`/special-offers` lists only `special === true` using the badged shared card; no coupons/countdown/deal-of-the-day/"% off"** — `getSpecial(60)` + defensive `special === true` re-filter → shared card grid ([SpecialOffers.js:150–152, :262–279](../src/pages/SpecialOffers/SpecialOffers.js)); grep of the changed files for coupon/countdown/deal-of-the-day/%-off/Buy-Now → matches only inside "removed"-explaining comments ([SpecialOffers.js:14](../src/pages/SpecialOffers/SpecialOffers.js), [SpecialOffers.module.css:5](../src/pages/SpecialOffers/SpecialOffers.module.css)).
- **Featured band uses `products.getFeatured()` + the shared card with an "Add to Enquiry List" icon-button** — `getFeatured(8)` ([Home.js:77](../src/pages/Home/Home.js)) → shared card; `FeaturedProducts.js` (the reusable band) now delegates to the same card ([:62–67](../src/components/FeaturedProducts/FeaturedProducts.js)).
- **A special product also appears in its normal category listing (NOT removed)** — the flag never filters a product out: `getSpecial` only *surfaces* the flagged set ([api.js:909–913 comment](../src/services/api.js)); the listing is filtered by `categoryId` downstream (untouched), so a special item still lists under its real category. (`11b`'s live `/products?category=wpc-louvers` → the special WPC louver; not re-executed here — see §8.)
- **Special items link to their normal PDP (`productPath`) and category (`categoryParam`)** — the shared card links `productPath(product)` ([ProductCard.js:54, :88](../src/components/storefront/ProductCard.js)); `productPath` prefers the slug ([helpers.js:103](../src/utils/helpers.js)).
- **Copy is enquiry-correct ("Special Products"/"curated", "Add to Enquiry List"); no deal/coupon/limited-time/Buy-Now language** — hero "Special Products" + "Curated picks" + "…available all year round, not limited-time deals" ([SpecialOffers.js:215–220](../src/pages/SpecialOffers/SpecialOffers.js)); the changed storefront files carry the forbidden terms only in "removed" comments.
- **Graceful empty state when no products are special** — `products.length === 0` → "No special products right now" → `/products` ([SpecialOffers.js:232–243](../src/pages/SpecialOffers/SpecialOffers.js)); `FeaturedProducts` renders `null` when empty ([:36](../src/components/FeaturedProducts/FeaturedProducts.js)); Home guards the band on `specialProducts.length > 0` ([:253](../src/pages/Home/Home.js)). (Seed has 10 specials, so the populated path renders.)
- **Brand blue `#1885d8` primary, gold `#fa9c4c` for the badge; premium/minimal; dual-mode identical in mock** — `--sf-color-primary = #1885d8`, `--sf-color-accent = #fa9c4c`, `--sf-gradient-primary = #1885d8→#1069b0` ([tokens :29, :35, :36](../src/theme/storefront-tokens.css)); blue hero + tabs, gold kicker + badge; pages read only through `apiService.*` + `extractData()`.

---

## 6. §11 KEEP-invariants — all intact

| Invariant (§11) | Re-grounded result | ✓ |
|---|---|---|
| **Dual-mode API + `extractData()`** | `getSpecial` keeps `IS_MOCK_API` branching + `extractData`; no `db.json` writes | ✅ |
| **Special is a badge, not a category** | Never filtered out of normal listings; `getSpecial` only surfaces the flag; `categoryId` untouched | ✅ |
| **`ProductCard` reuse** | The single canonical, domain-agnostic card; badge is additive/optional; the **two forked local cards were deleted, not multiplied** — `SpecialOffers`, `FeaturedProducts` and the homepage bands all render the one card | ✅ |
| **Enquiry-list wiring** | Cards add via `buildCartItem` → `addToCart` (localStorage `"cart"`); no purchase/coupon/wallet path | ✅ |
| **Enquiry-correct language** | Grep of the changed files for coupon/deal/countdown/%-off/Buy-Now/Add-to-Cart → visible-copy matches **zero**; the terms survive only in "removed" comments and untouched surfaces | ✅ |
| **Route stability** | `/special-offers` path unchanged ([App.js:118](../src/App.js)); no Special-Offers **admin** route re-added | ✅ |
| **Per-component CSS Modules + storefront/admin palette separation** | Preserved; storefront-only (no admin file in the diff); subtle framer-motion; skeleton + empty states kept | ✅ |

---

## 7. Findings — `11b` claims reconciled; one build-baseline reconciliation; no code defect

### 7.1 — RECONCILE (`11b` §5.1): `Home.js` repointed at the shared `getSpecial` — sanctioned by spec §6, regression-free
Prompt-10 left the homepage Special band on `getAll().filter(special).slice(0,8)`; Prompt-11 §6 requires the band and `/special-offers` to "share the same data source." Re-grounding confirms the minimal coordination: `Home.js` now calls `apiService.products.getSpecial(8)` ([:81](../src/pages/Home/Home.js)) and the redundant client-side `.filter` is gone, guards intact ([:94–96](../src/pages/Home/Home.js)). Same 8 specials, same shared card (now badged), no behavioural change — a sanctioned coordination, not a defect.

### 7.2 — RECONCILE (build baseline): `11b`'s −3.11/−1.89 delta is against `e353dff`, not `10c`'s `35bf669`
`11b` states the bundle "shrinks JS −3.11 kB → 384.05 kB, CSS −1.89 kB → 45.63 kB." A naïve reader comparing that against `10c`'s recorded Prompt-10 build (JS 387.31 kB / CSS 47.51 kB) would compute −3.26 kB JS and see an apparent ~0.15 kB discrepancy. Re-grounding resolves it: Prompt-11's **immediate parent is `e353dff`** ("Tidy Prompt-10 homepage leftovers"), not `35bf669`. A fresh build of `e353dff` yields **JS 387.16 kB / CSS 47.51 kB** — so the tidy commit shaved ~**0.15 kB JS** (removing the dead `styles.dark` reference `10c` §7.2 flagged and the skeleton tweak `10c` §7.3 flagged) while leaving CSS at **47.51 kB**. Against that true parent, Prompt-11 shrinks **−3.11 kB JS / −1.88 kB CSS**, reproducing `11b` exactly. Both notes are internally correct; the difference is entirely the intervening tidy commit. A documentary reconciliation, not a defect.

### 7.3 — CARRY-FORWARD (`11b` §5.2): the `/products` listing still uses its own "% OFF"/"Add to Cart" card (prompt 12 owns it)
The badge, the enquiry button and the clean price appear on **every surface that renders the shared card** — the homepage Featured/Special bands, `FeaturedProducts`, and `/special-offers`. The `/products` listing still renders its **own** card with a red `.discountBadge` "% OFF" and an "Add to Cart" button ([Products.js:938, :984](../src/pages/Products/Products.js)). That page is **prompt 12's** to move onto the shared card; when it does, the "Special" badge and enquiry-correct copy flow there with no further ProductCard change. The additive **data** rule already holds there. Not a Prompt-11 defect.

### 7.4 — CARRY-FORWARD (`11b` §5.3): the deals master-toggle, nav gating and deals **admin** are deliberately left intact
Per spec §4.4's stated preference, the `useDealsConfig` gate was **removed from `/special-offers`** (the collection always renders, with a graceful empty state). Re-grounding confirms the consequence `11b` recorded: the storefront nav **still** gates the `/special-offers` link on `useDealsConfig().enabled` — Header ([:75, :386](../src/components/Header/Header.js)), Footer ([:20, :65–67](../src/components/Footer/Footer.js)) and SidebarMenu (which filters the link out when `!dealsEnabled`, [:69–71](../src/components/SidebarMenu/SidebarMenu.js)) — and `DealsConfigContext`, `dealsConfig.js`, the `coupons` store and `AdminSpecialOffers` remain. So if an admin "disables deals," the nav link hides but the page still renders when reached directly. Removing the deals master-toggle, the nav gating and the deals **admin** entirely is owned by the admin-cleanup prompts (24/25/28/30); this prompt left them untouched to avoid stepping on those. Documented, not a defect. (These untouched surfaces — plus the PDP `PriceBlock`/`AddToCartBar`, `Checkout` coupons and `Wishlist`/`Products` cards — are why a whole-`src` grep for the forbidden terms is *expected* to hit; the §5/§6 check is scoped to the **changed** files, which are clean.)

### 7.5 — CARRY-FORWARD (`11b` §5.4): `FeaturedProducts.js` is now correct/reusable but still unmounted
Prompt-10 inlined the homepage product bands (rendering the shared card directly), so `FeaturedProducts.js` is imported nowhere — a fresh grep confirms it appears only in its own file. Prompt-11 §3 owns making it correct: it is now a thin, enquiry-correct band over the shared card (no local card, no "% OFF", no "Add to Cart"), genuinely reusable, but remains **unmounted**, so it changes no runtime AC. Kept (not deleted) because it is the sanctioned reusable Featured band the spec names; a later prompt may mount it. Not a defect.

### 7.6 — CARRY-FORWARD (`11b` §5.5): pricing display is still the plain `sellingPrice`
The shared card shows a clean single `sellingPrice` (`showSavings={false}`, no `comparePrice`); `priceType` `tiered`/`onEnquiry` products are not yet displayed differently — that is **prompt 15's** model. Special items are correctly framed as curated, never discounted. `PriceBlock` itself is untouched, so the PDP still shows a compare price until prompts 14/15. Not a Prompt-11 concern.

### 7.7 — NEW DOCUMENTARY NIT: the homepage Special band renders no skeleton (Featured does)
Re-grounding surfaces a small render-condition asymmetry inherited from Prompt-10 (Prompt-11 only repointed the data source, [:81](../src/pages/Home/Home.js)): the Featured band renders while `loading` and shows skeletons (`{(loading || featuredProducts.length > 0)}`, [:238](../src/pages/Home/Home.js)), but the Special band renders **only after load** (`{!loading && specialProducts.length > 0}`, [:253](../src/pages/Home/Home.js)) — so it shows nothing during the fetch rather than a shimmer. Purely cosmetic (both settle to the same badged grid), no layout break, out of Prompt-11's scope. Recorded, not a defect.

---

## 8. Build & runtime-scope note — reproduce exactly

- **Build (Prompt-11).** A fresh `CI=true npm run build` → **`Compiled successfully.`** (exit 0, warnings-as-errors — so it also proves the deletions left no orphaned imports), JS `main.1f7c74c9.js` = **384.05 kB gzip**, CSS `main.872a712d.css` = **45.63 kB gzip** — reproducing `11b`'s absolute figures byte-for-byte.
- **Build (parent `e353dff`).** A second build of the immediate parent → **`Compiled successfully.`**, JS `main.6fe82c13.js` = **387.16 kB gzip**, CSS `main.2088bae1.css` = **47.51 kB gzip**. Against it, Prompt-11 shrinks **−3.11 kB JS / −1.88 kB CSS** — independently reproducing `11b`'s delta (mostly the deleted coupons/countdown/deal-of-the-day machinery + two forked local cards). The diff's **434 ins / 1,721 del (−1,287 lines)** across 8 files reproduces exactly. `build/` is gitignored, so both rebuilds left the tracked tree clean and HEAD returned to the branch.
- **Runtime scope.** `11b`'s *structural* live claims (`/special-offers` h1 + kicker + hero, the badged shared cards, the enquiry button, curation tabs, PDP links, the shared `getSpecial` source, both bands' 16 cards) are re-grounded here **from source + seed + build** rather than a fresh browser run — consistent with `06c`–`10c`. The **browser-only** observations (`getComputedStyle` → `rgb(24,133,216)` / `rgb(250,156,76)`, the add-to-enquiry `localStorage` increment, the exact "9 badges across 16 cards" / "10 tabs" counts, the additive `/products?category=wpc-louvers` check, "console error-free") were **not** re-executed this pass; their structural basis is confirmed — the blue/gold tokens resolve to `#1885d8`/`#fa9c4c` in source, every imported symbol resolves, the `buildCartItem` → `useCart` add path is intact, the seed carries 10 specials, and the build compiles with no errors.

---

## 9. Conclusion

The committed Prompt-11 Featured & Special Products work is **faithful to the live repository, its own spec, and the `11b` post-execution note**. All nine §9 acceptance criteria reproduce under an independent re-run — the additive gold `#fa9c4c` "Special" ribbon on the one canonical `storefront/ProductCard` (top-left, `pointer-events:none`, `special === true` only, no heart overlap), the dual-mode `getSpecial` mirroring `getFeatured` as the single shared source for the homepage band (`getSpecial(8)`) and the `/special-offers` collection (`getSpecial(60)` → 10 badged shared cards), `FeaturedProducts` repurposed into a thin reusable band over the shared card, and `/special-offers` rebuilt into the curated Special Products collection (blue `--sf-gradient-primary` hero, gold "Curated picks" kicker, curation `CategoryTabs`, badged grid, skeletons, graceful empty → `/products`) with no coupons / countdown / deal-of-the-day / "% off" urgency. The §11 KEEP invariants (dual-mode `apiService` + `extractData`, special-as-a-badge, shared-card reuse with the two forked cards deleted, enquiry-list `buildCartItem` wiring, enquiry-correct language, route stability, CSS-Module/palette separation) all hold. Two fresh production builds compile cleanly and reproduce the note's **−3.11 kB JS / −1.88 kB CSS** shrink (net −1,287 source lines across 8 files) against the true parent `e353dff`. The seven findings are reconciliations/nuances, not defects: the sanctioned shared-`getSpecial` repoint (§7.1), a build-baseline reconciliation showing `11b`'s delta is measured against the tidied `e353dff`, not `10c`'s `35bf669` (§7.2), and the carry-forwards owned by prompts 12/15 and the admin-cleanup prompts 24/25/28/30 (§7.3–7.6), plus a cosmetic homepage skeleton asymmetry inherited from Prompt-10 (§7.7). Prompt-11 is enquiry-correct and behaviourally sound.

---

*Re-grounding complete against the live `storefront/ProductCard.js`/`.module.css`, `services/api.js` (`getSpecial`), `FeaturedProducts.js`/`.module.css`, `SpecialOffers.js`/`.module.css`, `Home.js` and the consumed `utils/helpers.js`, `utils/categories.js`, `theme/storefront-tokens.css`, `storefront/PriceBlock`, `db.json` (2026-07-02). No files changed except this note. Additive gold `#fa9c4c` "Special" ribbon (`special === true`, top-left, `pointer-events:none`, no heart overlap) · dual-mode `getSpecial` (mock `/products?special=true`, Laravel `/products/special` via `extractData`) shared by homepage `getSpecial(8)` + `/special-offers` `getSpecial(60)` · `db.json` = 10 `special:true` + 10 `featured:true` → 16 homepage cards / 10 collection cards · `FeaturedProducts` = thin reusable band over the shared card (unmounted) · `/special-offers` = blue `--sf-gradient-primary` hero + gold "Curated picks" kicker + curation `CategoryTabs` + badged grid + skeletons + empty→`/products`, no coupons/countdown/deal-of-the-day/%-off · `buildCartItem` → `useCart`, `localStorage["cart"]` unchanged · nav still gates `/special-offers` on `useDealsConfig().enabled` (admin cleanup owns removal) · commit `d922868` = 8 files, 434/1,721 (−1,287), parent `e353dff` · `CI=true react-scripts build` → Compiled successfully, JS 384.05 kB / CSS 45.63 kB, −3.11 kB JS / −1.88 kB CSS vs parent `e353dff` (387.16 / 47.51) · working tree clean. Seven reconciliations/nuances in §7 — no code defect.*
